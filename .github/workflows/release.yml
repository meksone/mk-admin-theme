name: Release Plugin ZIP

on:
  release:
    types: [published]

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4

      - name: Build ZIP with canonical folder name
        run: |
          mkdir -p build/mk-admin-theme
          rsync -av \
            --exclude='.git' \
            --exclude='.github' \
            --exclude='build' \
            . build/mk-admin-theme/
          cd build && zip -r mk-admin-theme.zip mk-admin-theme/

      - name: Attach ZIP to release
        uses: softprops/action-gh-release@v2
        with:
          files: build/mk-admin-theme.zip
